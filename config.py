botconf = dict(
    server= "localhost",
    port = "6667",
    channel = "#test",
    botnick = "running", #this should probably be the same as the machine's hostname, but I'm not going to tell you what to do
)

